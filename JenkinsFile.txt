pipeline {
    agent {
	    docker { image 'ubuntu' }
    }
            environment { HOME = "${env.WORKSPACE}" }
    stages {
        stage('Checkout') {
            steps {
                // Get some code from a GitHub repository
                git 'https://github.com/rocky19devops/JenkinsPipline.git'
                }
            }
        stage('Install pip modules') {
            steps {
	        // ******* Required change
		     sh 'apt-get install '
                // Install python modules
                     sh 'pip install flask --user'
	        }
            }	
        stage('Build') {
            steps {
	        sh 'which docker'
                sh 'docker build -t rockyapp:v1 .'
                }
            }
        stage('Push') {
            steps {
                echo "Pushed Successfully"
                }
            }
        }
    }
